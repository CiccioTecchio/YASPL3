head
int input, fattoriale;

def calcola(){
    int i = 1;
    while( i <= input ) do {
        fattoriale = i*fattoriale;
        i = i + 1;
    }
}

start
fattoriale = 1;
"Inserisci un intero positivo per calcolare il fattoriale\n" ->;
input<-;
if(input >=1) then {
    calcola();    
    "Il fattoriale è ",fattoriale,"\n"->;
}else {
    "Devi inserire un intero positivo!\n" ->;
}
